- Add support for lazy loading by annotation lookup
- Read only sections of wav file (i.e. from onset offset) 
- Add function to read only the metadata -- numchan,  frames, samples, bitrate, similar to _read_wav, then close the file 

